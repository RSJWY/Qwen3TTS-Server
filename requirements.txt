# Core
# IMPORTANT: vllm-omni 0.18.0 is built for vllm 0.18.x only.
# vllm 0.19+ reorganized modules (e.g., vllm.inputs.data removed),
# which breaks vllm-omni imports. Pin to 0.18.x until vllm-omni
# releases a compatible version.
vllm>=0.18.0,<0.19.0
vllm-omni>=0.18.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
pydantic>=2.0

# Audio
soundfile>=0.12.0
numpy>=1.24.0

# ML (vLLM handles torch, but these are needed for engine)
torch>=2.0.0
